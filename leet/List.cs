using System;
using System.Collections.Generic;
using System.Text;

namespace leet
{
    class MyList
    {
        List<int> ls = new List<int>();

        MyList()
        {
            ls.Add(10);
        }

        public AddNumberToFirst()
        {
            AddValue(ls[0]);   
        }
        public void AddValue(ref int t)
        {

        }
    }
}
